server:
  port: 8080
  tomcat:
    threads:
      min-spare: 20

spring:
  threads:
    virtual:
      enabled: true  # Java 21 Virtual Thread 활성화 — 블로킹 렌더링 작업에서 OS 스레드 낭비 방지
  web:
    resources:
      static-locations: classpath:/public/
      cache:
        cachecontrol:
          max-age: 3600
